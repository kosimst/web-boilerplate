import { css } from 'lit-element'
import { primary } from '../../constants/theme'

export default css`
  :host {
    --material-primary-color: ${primary};
    --material-primary-text-color: ${primary};
  }
`
